@implements IDisposable
@using LMS.Blazor.Client.Components.Icons
@inject NavigationManager NavigationManager

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center gap-2" href="">
            <BookOpenIcon Size="28" CssClass="text-primary" />
            <span class="h5 mb-0">Lexicon LMS</span>
        </a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <AuthorizeView Roles="Teacher">
            <Authorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="teacher/dashboard">
                        <DashboardIcon /> Dashboard
                    </NavLink>
                </div>

                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="teacher/managecourses">
                        <BookOpenIcon CssClass="px-2" /> Manage Courses
                    </NavLink>
                </div>

                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="manageusers">
                        <ParticipantsIcon CssClass="px-2" /> Manage Users
                    </NavLink>
                </div>

                <form action="Account/Logout" method="post" class="p-4">
                    <AntiforgeryToken />
                    <input type="hidden" name="ReturnUrl" value="@currentUrl" />
                    <button type="submit" class="nav-link">
                        <LogOutIcon CssClass="text-danger px-2" /> Logout
                    </button>
                </form>
            </Authorized>
        </AuthorizeView>

        <AuthorizeView Roles="Student">
            <Authorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="student/dashboard">
                        <DashboardIcon /> Dashboard
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="student/mycourse">
                        <BookOpenIcon /> My Course
                    </NavLink>
                </div>

                <form action="Account/Logout" method="post" class="p-4">
                    <AntiforgeryToken />
                    <input type="hidden" name="ReturnUrl" value="@currentUrl" />
                    <button type="submit" class="nav-link">
                        <LogOutIcon Size="28" CssClass="text-danger" /> Logout
                    </button>
                </form>
            </Authorized>
        </AuthorizeView>
    </nav>
</div>

@code {
    private string? currentUrl;

    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}

